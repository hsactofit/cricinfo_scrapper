# Machine 1 — M4 Air
# Copy this file to .env on M4 Air

MACHINE_ID=m4_air
CHROME_VERSION=146
WORKERS=4
PAGE_DELAY=2.0

# DB not needed on this machine (only --init and --parse need DB, both run on M2 Mini)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=krickbiz
DB_USER=himanshu
DB_PASSWORD=admin
